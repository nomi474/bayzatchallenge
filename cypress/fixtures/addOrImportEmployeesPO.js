const addOrImportEmployeesPO = {
           //:visible is needed for correctly identifying the button
    addEmployees:"a[href='/enterprise/dashboard/employees/create']:visible"
};

export default addOrImportEmployeesPO